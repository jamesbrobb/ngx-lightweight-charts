
describe('TimescaleStreams', () => {

  describe('visibleTimeRangeChange$', () => {

    it('should expose the timescale objects visible time range change values', () => {

    });
  });

  describe('visibleLogicalRangeChange$', () => {

    it('should expose the timescale objects visible logical range change values', () => {

    });
  });

  describe('sizeChange$', () => {

    it('should expose the timescale objects size change values', () => {

    });
  });

  describe('destroy', () => {

    it('should unsubscribe all streams', () => {
     
    });
  });
});
