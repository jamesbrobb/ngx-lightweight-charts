import { ChartsCollectorDirective } from './charts-collector.directive';

describe('ChartsCollectorDirective', () => {
  it('should create an instance', () => {
    const directive = new ChartsCollectorDirective();
    expect(directive).toBeTruthy();
  });
});
