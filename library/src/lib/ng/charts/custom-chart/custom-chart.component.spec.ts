import { ComponentFixture, TestBed } from '@angular/core/testing';

import { TVChartCustomSeriesComponent } from './custom-chart.component';
import {Time} from "lightweight-charts";

describe('TVChartCustomSeriesComponent', () => {
  let component: TVChartCustomSeriesComponent;
  let fixture: ComponentFixture<TVChartCustomSeriesComponent>;

  /*beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [TVChartCustomSeriesComponent]
    })
    .compileComponents();

    fixture = TestBed.createComponent(TVChartCustomSeriesComponent<Time>);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });*/

  it('should create', () => {
    //expect(component).toBeTruthy();
  });
});
