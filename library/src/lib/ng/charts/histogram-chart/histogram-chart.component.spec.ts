import { ComponentFixture, TestBed } from '@angular/core/testing';

import { TVHistogramChartComponent } from './histogram-chart.component';

describe('HistogramChartComponent', () => {
  let component: TVHistogramChartComponent;
  let fixture: ComponentFixture<TVHistogramChartComponent>;

  /*beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [TVHistogramChartComponent]
    })
    .compileComponents();

    fixture = TestBed.createComponent(TVHistogramChartComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });*/

  it('should create', () => {
    //expect(component).toBeTruthy();
  });
});
